spring:
  application:
    name: demo-config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:cypexa/config-repo.git
          clone-on-start: true
          ignore-local-ssh-settings: true


server:
  port: 8071